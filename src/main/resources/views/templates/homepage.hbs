<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accounts</title>
    <style>
        /* Styles for the navigation bar */
        /* Style the navigation menu */
        .navbar {
            width: 100%;
            background-color: #555;
            overflow: auto;
            display: flex;
            justify-content: center;
            role: navigation; /* Added role for semantic meaning */
        }
        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        /* Navigation links */
        .navbar a {
            padding: 12px;
            color: white;
            text-decoration: none;
            font-size: 17px;
            width: 25%;
            text-align: center;
            display: inline-block;
            cursor: pointer;
            outline: none; /* Remove default outline */
        }

        /* Add a background color on mouse-over */
        .navbar a:hover {
            background-color: #000;
        }

        /* Add focus styles */
        .navbar a:focus {
            background-color: #888;
        }

        /* Additional styles to make the page look better */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .content {
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-left: 2%;
            margin-right: 5%;
            margin-top: 3%;
        }

        #welcome-title {
            margin-left: 2%;
        }
    </style>
</head>
<body>

<div class="navbar" aria-label="Main Navigation">
    <a href="/homepage" tabindex="0">Account</a>
    <a href="/transactions" tabindex="0">Transactions</a>
    <a href="/spending" tabindex="0">Spending</a>
    <a href="/" tabindex="0" id="logout-button">Logout</a>
</div>

<h1 id="welcome-title">Welcome back {{fromPost}}</h1>
<div class="content">
    <p>Account Details:</p>
    <ul>
        <li>Current Balance: £{{currentAmount}}</li>
        <li>Sort-Code: {{sortCode}}</li>
        <li>Account-Number: {{accountNumber}}</li>
    </ul>

    <p>Round-Up Enabled: {{#if account.roundUpEnabled}}Yes{{else}}No{{/if}}</p>
    {{#if account.roundUpEnabled}}
        <p>Round-Up Pot: £{{account.roundUpPot}}</p>
    {{/if}}
</div>

</body>


</html>